# Contributing

Use Python 3.10 or newer. Create a virtual environment, install `.[dev]`, and run `pytest` before opening a pull request. Keep generated data, checkpoints, credentials, and learner-identifying information out of commits. Changes to a data schema or CLI must update the README and tests. Report privacy or security concerns privately to the repository owner rather than opening a public issue containing sensitive data.
